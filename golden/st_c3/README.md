ST-C3 golden-case library.

Each `case_*.json` file is a deterministic, canonical funnel example with:
- fully specified evidence inputs
- expected state-machine path
- expected rejection, if any
- expected trade-plan subset, if valid
